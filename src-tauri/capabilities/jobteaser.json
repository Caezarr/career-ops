{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "jobteaser-auth",
  "description": "Allow the JT auth WebView to invoke a narrow set of Tauri commands so the bridge script can finalize SSO capture. Scoped to JT's domain only. Custom commands (jobteaser_*) are NOT gated by this ACL — see audit 2026-05-05 CRITICAL #1; runtime window-label assertions are tracked as follow-up.",
  "windows": ["jobteaser-auth"],
  "remote": {
    "urls": ["https://*.jobteaser.com/*", "https://jobteaser.com/*"]
  },
  "permissions": [
    "core:window:allow-close",
    "core:event:default"
  ]
}
